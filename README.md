# Школьная информационная система

Веб-приложение на Flask для управления школьной информационной системой с системой аутентификации.

## Возможности

- 🔐 Система аутентификации (регистрация и вход)
- 📚 Управление студентами (добавление, просмотр)
- 🏫 Управление классами (добавление, просмотр)
- 📝 Управление оценками (добавление, просмотр)
- 📊 Статистика в админ-панели
- 🔒 Защищенные админские маршруты

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
python app.py
```

3. Откройте браузер и перейдите по адресу:
```
http://127.0.0.1:5000
```

4. На главной странице зарегистрируйте новый аккаунт или войдите в существующий

### Учетные данные по умолчанию

При первом запуске автоматически создается администратор:
- **Email:** `admin@school.ru`
- **Пароль:** `admin123`

⚠️ **Важно:** Измените пароль после первого входа в продакшене!

## Структура проекта

```
flaskmanpage/
├── app.py                 # Основной файл приложения
├── requirements.txt       # Зависимости проекта
├── data.json             # Файл для хранения данных (создается автоматически)
├── users.json            # Файл для хранения пользователей (создается автоматически)
├── templates/            # HTML шаблоны
│   ├── public_index.html # Публичная главная страница
│   ├── login.html        # Страница входа
│   ├── register.html     # Страница регистрации
│   ├── admin/            # Админские шаблоны
│   │   ├── base.html
│   │   └── index.html
│   ├── students.html
│   ├── add_student.html
│   ├── classes.html
│   ├── add_class.html
│   ├── grades.html
│   └── add_grade.html
└── static/
    └── css/
        └── style.css     # Стили приложения
```

## Цветовая схема

- **F6903B** - Оранжевый (основной акцент)
- **181A49** - Темно-синий (навигация, заголовки)
- **206EB5** - Синий (вторичные элементы)
- **6AB7FD** - Светло-синий (акценты)

## Использование

### Публичная часть
1. **Главная страница** (`/`) - публичная страница с информацией о системе
2. **Регистрация** (`/register`) - создание нового аккаунта
3. **Вход** (`/login`) - вход в систему

### Админ-панель (требует авторизации)
1. **Панель управления** (`/admin`) - статистика и быстрые действия
2. **Студенты** (`/admin/students`) - просмотр списка студентов и добавление новых
3. **Классы** (`/admin/classes`) - просмотр классов и добавление новых
4. **Оценки** (`/admin/grades`) - просмотр оценок и добавление новых

## Примечания

- Данные хранятся в файлах `data.json` и `users.json` (в продакшене рекомендуется использовать базу данных)
- Пароли хранятся в хешированном виде (SHA-256)
- Все админские маршруты защищены и требуют авторизации
- Приложение работает в режиме отладки (debug mode)
- Для продакшена измените `secret_key` в `app.py` на случайную строку

